if (window.angular && window.angular.env && window.angular.env.QT_UI_LOG) {
  window.console.log('qt-angular-ui/src/radio load');
}

import './index.scss';

import component   from './_index';

export default component;