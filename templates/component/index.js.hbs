import './index.scss';

import angular    from 'angular';
import Controller from './index.controller';
import Template   from './index.jade';
import Components from './components';

export default angular.module('{{#if ns/camelcase}}{{ ns/camelcase }}.{{/if}}{{ names/camelcase }}', [
  Components,
])
.config(function ($stateProvider) {
  'ngInject';

  $stateProvider
  .state('{{#if ns/camelcase}}{{ ns/camelcase }}.{{/if}}{{ names/camelcase }}', {
    url        : '{{ names/underscore }}/',
    template   : Template,
    controller : Controller,
  });
})
.name;
