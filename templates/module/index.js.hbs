import './index.scss';

import angular    from 'angular';
import Common     from 'common';
import Components from './components';
import Template   from './home.jade';

export default angular.module('{{ names/camelcase }}', [
  Common,
  Components,
])
.config(function ($stateProvider, $urlRouterProvider) {
  'ngInject';

  $stateProvider
  .state('{{ names/camelcase }}', {
    abstract : true,
    url      : '/{{ names/underscore }}/',
    template : Template,
  });

  $urlRouterProvider.when('/', '/{{ names/underscore }}/');
})
/**
 * Run before above resolve url
 * You can configure something,
 * write some common async logic.
 */
.run(function ($urlRouter) {
  $urlRouter.listen();
})
.name;
